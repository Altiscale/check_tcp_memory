require 'spec_helper'

describe CheckTcpMemory do
  it 'has a version number' do
    expect(CheckTcpMemory::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
